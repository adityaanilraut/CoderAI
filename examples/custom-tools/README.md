# Example: Custom Tools

Demonstrates a custom `kosong` tool (`Ls`) registered directly on the
agent toolset. Run from the repo root so `coderai` is importable:

```sh
cd examples/custom-tools
python3 main.py
```
