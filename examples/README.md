# Examples

Minimal, runnable CLI recipes demonstrating CoderAI features.

| Example | Shows |
| ------- | ----- |
| `coderai-cli-stream-json` | Drive `coderai --print` via stream-JSON over stdio |
| `sample-plugin` | Language-agnostic plugin (`plugin.json` + `SKILL.md`) |
